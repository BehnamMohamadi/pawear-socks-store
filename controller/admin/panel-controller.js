const resources=require('../../services/admin/resources');const {AppError}=require('../../utils/app-error');const Payment=resources.payments.model,Order=resources.orders.model,Product=resources.products.model;const {expireStalePayments}=require('../../services/shopping-services/payment-service');
exports.dashboard=async(req,res)=>{await expireStalePayments();const [users,products,boxes,orders,reviews,sales,lowStock,recent]=await Promise.all([resources.users.model.countDocuments(),Product.countDocuments({isActive:true}),resources.boxes.model.countDocuments({isActive:true}),Order.countDocuments(),Payment.countDocuments({requiresReview:true,reviewStatus:{$ne:'resolved'}}),Order.aggregate([{$match:{paymentStatus:'paid'}},{$group:{_id:null,total:{$sum:'$totalAmount'}}}]),Product.find({isActive:true,stock:{$lte:5}}).sort('stock').limit(10).lean(),Order.find().sort('-createdAt').limit(10).populate('user','firstname lastname').lean()]);const actionCounts={packing:await Order.countDocuments({status:'confirmed',paymentStatus:'paid',packedAt:null}),ready:await Order.countDocuments({status:'confirmed',paymentStatus:'paid',packedAt:{$ne:null}}),shipped:await Order.countDocuments({status:'shipped'}),reviews};res.render('admin/pawear/dashboard',{title:'داشبورد مدیریت',adminUser:req.user,metrics:{'فروش پرداخت‌شده (تومان)':sales[0]?.total||0,'سفارش‌ها':orders,'جوراب‌های فعال':products,'باکس‌های فعال':boxes,'کاربران':users,'پرداخت نیازمند بررسی':reviews},lowStock,recent,actionCounts});};
exports.list=async(req,res)=>{const key=req.params.resource,config=resources[key];if(!config)throw new AppError(404,'بخش پیدا نشد.');if(['payments','orders'].includes(key))await expireStalePayments();const page=Math.max(1,parseInt(req.query.page,10)||1),q=typeof req.query.q==='string'?req.query.q.trim().slice(0,100):'',filter={};if(q){const pattern={$regex:q.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),$options:'i'};if(['products','boxes','brands'].includes(key))filter.$or=[{name:pattern},...(key==='products'||key==='boxes'?[{sku:pattern}]:[])];else if(key==='orders')filter.$or=[{orderNumber:pattern},{'shippingAddressSnapshot.recipientName':pattern},{'shippingAddressSnapshot.recipientPhone':pattern},{'shippingAddressSnapshot.city':pattern}];else if(key!=='carts'&&key!=='payments')filter[key==='users'?'phonenumber':'name']=pattern;}if(['products','boxes','brands','categories','subcategories'].includes(key)&&['true','false'].includes(req.query.active))filter.isActive=req.query.active==='true';if(key==='products'&&/^[a-f0-9]{24}$/i.test(req.query.brand||''))filter.brandId=req.query.brand;if(['orders','payments'].includes(key)&&req.query.status)filter.status=String(req.query.status).slice(0,40);if(key==='payments'&&req.query.review==='true')filter.requiresReview=true;if(key==='orders'&&['packing','ready'].includes(req.query.stage)){filter.status='confirmed';filter.paymentStatus='paid';filter.packedAt=req.query.stage==='ready'?{$ne:null}:null;}if(key==='products'&&['low','empty'].includes(req.query.stock)){filter.isActive=true;filter.stock=req.query.stock==='empty'?0:{$lte:5};}let query=config.model.find(filter).sort(key==='orders'&&(req.query.stage||req.query.status==='confirmed')?'createdAt':'-createdAt').skip((page-1)*25).limit(25);if(['orders','payments','carts'].includes(key))query=query.populate('user','firstname lastname phonenumber');if(key==='products')query=query.populate('brandId','name').populate('category','name').populate('subCategory','name');let [rows,total,brands]=await Promise.all([query.lean(),config.model.countDocuments(filter),resources.brands.model.find({}).sort('name').lean()]);if(key==='brands'){const counts=await Product.aggregate([{$group:{_id:'$brandId',count:{$sum:1}}}]);const map=new Map(counts.map(x=>[String(x._id),x.count]));rows=rows.map(r=>({...r,productCount:map.get(String(r._id))||0}));}if(['categories','subcategories'].includes(key)){const field=key==='categories'?'category':'subCategory',counts=await Product.aggregate([{$group:{_id:'$'+field,count:{$sum:1}}}]);const map=new Map(counts.map(x=>[String(x._id),x.count]));rows=rows.map(r=>({...r,productCount:map.get(String(r._id))||0}));}const stageCounts=key==='orders'?{packing:await Order.countDocuments({status:'confirmed',paymentStatus:'paid',packedAt:null}),ready:await Order.countDocuments({status:'confirmed',paymentStatus:'paid',packedAt:{$ne:null}}),shipped:await Order.countDocuments({status:'shipped'}),delivered:await Order.countDocuments({status:'delivered'})}:{};const pageUrl=n=>'/admin/'+key+'?'+new URLSearchParams({...Object.fromEntries(Object.entries(req.query).filter(([,v])=>typeof v==='string')),page:n});res.render(['orders','payments'].includes(key)?'admin/pawear/operations':'admin/pawear/list',{title:config.title,adminUser:req.user,key,config,rows,page,total,q,filters:req.query,pageUrl,stageCounts,brands});};
exports.form=async(req,res)=>{const key=req.params.resource,config=resources[key];if(!config||config.readonly)throw new AppError(404,'فرم پیدا نشد.');let record={isActive:true,stock:0,price:1,discount:0,sortOrder:0,gender:'unisex',role:'user',variants:[]};if(req.params.id){if(!/^[a-f0-9]{24}$/i.test(req.params.id))throw new AppError(404,'رکورد پیدا نشد.');record=await config.model.findById(req.params.id).lean();if(!record)throw new AppError(404,'رکورد پیدا نشد.');}const [categories,subcategories,products,brands]=await Promise.all([resources.categories.model.find().sort('sortOrder').lean(),resources.subcategories.model.find().sort('sortOrder').lean(),key==='boxes'?Product.find().select('name sku price stock isActive variants sizes').sort('name').lean():[],resources.brands.model.find().sort('name').lean()]);res.render('admin/pawear/form',{title:(record._id?'ویرایش ':'افزودن ')+config.title,adminUser:req.user,key,config,record,categories,subcategories,products,brands});};
exports.detail=async(req,res)=>{const key=req.params.resource,config=resources[key];if(!config?.readonly||!/^[a-f0-9]{24}$/i.test(req.params.id))throw new AppError(404,'رکورد پیدا نشد.');await expireStalePayments();const record=await config.model.findById(req.params.id).select(key==='orders'?'+packingNote':'').populate('user','firstname lastname phonenumber email').lean();if(!record)throw new AppError(404,'رکورد پیدا نشد.');const payments=key==='orders'?await Payment.find({order:record._id}).sort('-createdAt').lean():[];res.render('admin/pawear/detail',{title:'جزئیات '+config.title,adminUser:req.user,key,config,record,payments});};
