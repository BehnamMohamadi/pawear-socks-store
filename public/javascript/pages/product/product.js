(() => {const image=document.querySelector('#mainProductImage');document.querySelectorAll('[data-gallery-src]').forEach(button=>button.addEventListener('click',()=>{image.src=button.dataset.gallerySrc;document.querySelectorAll('[data-gallery-src]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));}));const input=document.querySelector('#productQuantity');const adjust=delta=>{input.value=Math.max(1,Math.min(Number(input.max),Number(input.value||1)+delta));};document.querySelector('[data-quantity-plus]')?.addEventListener('click',()=>adjust(1));document.querySelector('[data-quantity-minus]')?.addEventListener('click',()=>adjust(-1));})();

(() => {const select=document.querySelector('#productSize');if(!select)return;const button=document.querySelector('.buy-button'),quantity=document.querySelector('#productQuantity');const update=()=>{const option=select.selectedOptions[0];const stock=Number(option?.dataset.stock||0);button.disabled=!select.value||stock<1;quantity.disabled=button.disabled;quantity.max=Math.min(100,Math.max(1,stock));quantity.value=1;if(select.value)document.querySelector('[data-selected-price]').textContent=Number(option.dataset.price).toLocaleString('fa-IR')+' تومان';};select.addEventListener('change',update);update();})();
